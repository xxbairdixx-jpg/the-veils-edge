# 2026-03-15 Session Notes

## GitHub Setup
- Username: `xxbairdixx-jpg`
- Email: `xxbairdixx@gmail.com`
- Git configured globally with name "xXBairdiXx"
- Repo name: `dnd-tools` (to be created as **public** for free GitHub Actions)
- PAT token created (stored in ~/.git-credentials)
- Repo NOT yet created on GitHub — user needs to do github.com/new → "dnd-tools" → Public → No README

## GitHub Actions Workflow
- Created `.github/workflows/build-apks.yml` at ~/dnd-tools/
- Builds both VTT Android and Jarvis Companion APKs
- Trigger: manual (workflow_dispatch) or push to main
- APKs downloadable from Actions tab as artifacts (90-day retention)

## Android Build Issue
- **Cannot build APKs directly on Termux/Android** — AAPT2 binary is x86 Linux, won't run on ARM64
- Must use GitHub Actions or Android Studio on PC for builds
- Android SDK installed at ~/android-sdk but only usable for development, not building

## Jarvis Companion (Web App)
- Files: ~/voice-profiles/jarvis/index.html + server.py
- Backend: Flask on port 8090 (connects to LM Studio GLM 4.6V Flash)
- Fixed: backend now strips GLM thinking tags (<think></think>, <|begin_of_box|>, etc.)
- Fixed: PTT button handlers were missing — added mousedown/mouseup/touch events
- Fixed: Camera variables (cameraVideo, cameraCanvas, cameraModal) were undeclared
- Fixed: cameraModal used wrong ID ('camera-preview' vs actual 'camera-modal')
- **Known limitation**: PTT (webkitSpeechRecognition) and camera (getUserMedia) require HTTPS or localhost — won't work when accessing from another device via IP
- Served from ~/voice-profiles/vtt/ via symlink at port 8080

## VTT (Web)
- Files: ~/voice-profiles/vtt/index.html
- Command server: ~/voice-profiles/vtt/command-server.py (port 9000)
- Voice DM: ~/voice-profiles/voice-dm.py (PTT port 9001, commands port 9000)
- Served from ~/voice-profiles/ via Python HTTP server on port 8080 (bound to 0.0.0.0)
- **Key fix**: Termux HTTP servers must bind to 0.0.0.0, not default localhost

## Voice DM Architecture
- PTT input: WebSocket on port 9001
- VTT commands: HTTP POST to port 9000
- VTT polls commands every 500ms via JavaScript fetch
- LM Studio API at 192.168.0.222:1234/v1/chat/completions
- GLM 4.6V Flash model on GPU

## Android Projects (source code only, no APKs yet)
- VTT: ~/vtt-android/ (13 files, Kotlin)
- Jarvis: ~/jarvis-android/ (19 files, Kotlin)
- Both committed to ~/dnd-tools/ repo, ready to push to GitHub

## Known Issues
- Port conflicts are common when restarting services — use `pkill -9 -f python3` carefully (kills everything including OpenClaw)
- Python HTTP server (`python3 -m http.server`) in Termux binds to localhost only — need custom script with 0.0.0.0
- Subagents sometimes create files with errors (missing click handlers, undeclared variables, wrong IDs) — always verify generated code
- GLM thinking tags come in multiple formats: <think>, <|begin_of_box|> — need to strip all
- PyPDF2 can read PDF form field values from fillable PDFs
